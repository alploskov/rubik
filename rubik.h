#include <stdint.h>

#define if if (
#define elsif } else if

#define els } else {
#define then ) {


#define for for (int* _ = &
#define in ;
#define range(finish) *_ < finish; (*_)++
#define do ) {

#define continue continue;
#define break break;

#define end }

#define main int main() {

typedef int def_i32;
typedef int i32;

typedef uint32_t def_u32;
typedef uint32_t u32;
